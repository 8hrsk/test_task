config:
  target: "http://localhost:3001"
  phases:
    - duration: 10
      arrivalRate: 1000
scenarios:
  - flow:
    - post:
        url: "/balance/update"
        json:
          userId: 1
          amount: -2
